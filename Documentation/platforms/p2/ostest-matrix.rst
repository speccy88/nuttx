ostest matrix
=============

Status: DRAFTED.

The ``p2-ec32mb:ostest`` configuration selects ``CONFIG_TESTING_OSTEST``. Each applicable flat uniprocessor ostest group is HIL-REQUIRED for runtime. Cloud objective is compilation only; no row may be PASS until physical hardware runs it. Detailed per-source enumeration remains BLOCKED pending cloned pinned ``../apps`` inspection.
